define([], function() {
    describe("options has jobs", function() {
      it("contains jobs", function() {
        expect(1).toEqual(1);
        // expect(options.jobs).toBe(true);
      });
    });
});